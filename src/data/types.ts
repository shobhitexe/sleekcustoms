export type navLinksType = {
  title: string;
  link: string;
};

export type servicesDataType = {
  title: string;
  image: string;
};

export type partnersImagesType = {
  alt: string;
  image: string;
};

export type reviewsDataTypes = {
  title: string;
  description: string;
  name: string;
  logo: string;
};
